## DAY 02 — REINFORCEMENT (LOGIC → CODE)

Today I focused on strengthening my ability to translate logic into C code quickly and correctly.

The goal of this session was not to learn new concepts, but to reinforce what I already know and push my logic further through variations of exercises.

## What I worked on:

- Nested loops (while inside while)
- Writing clean and structured conditions (if, &&, ||)
- Controlling output format precisely using write
- Avoiding repetition using logic (i < j, ordered combinations)
- Applying simple math conditions (sum, even, prime)
- Thinking before coding instead of guessing

## Exercises completed:

- ft_print_pairs_increasing → understanding ordered pairs
- ft_print_pairs_no_repeat → avoiding duplicates (i < j)
- ft_print_comb2 → combinations of 2 digits
- ft_print_comb3 → combinations of 3 digits
- ft_print_comb4 → combinations of 4 digits
- ft_print_pairs_sum → filtering with a condition (i + j == 10)
- ft_print_pairs_even_sum → using modulo for even conditions
- ft_print_pairs_prime_sum → applying logical filtering with prime numbers
- ft_print_mirror_pairs → understanding symmetry in output

## What I improved:

- I got faster at writing nested loops without confusion
- I started thinking about conditions before coding
- I became more precise with output formatting (commas, spaces, last element)
- I can now adapt one exercise into many variations easily

## Mistakes I noticed:

- Sometimes I rely on “it works” instead of understanding deeply
- I need to be more careful with the last case (no extra comma)
- I occasionally use unnecessary operations instead of simple logic

## Conclusion:

Today I made real progress in logic.

I am no longer just writing code — I am starting to understand how to control it.

This reinforcement is helping me become faster, cleaner, and more confident.

## Next step:

I will move forward to strings, keeping the same mindset:
understand → practice → reinforce → move on.